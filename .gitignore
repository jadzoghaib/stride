__pycache__/
*.pyc
.venv/
data/
node_modules/
dist/
.env
.env.local
data/media/

# TypeScript incremental-build cache: regenerated by every `tsc -b`, so
# tracking it means a clean build shows up as a dirty tree.
*.tsbuildinfo
